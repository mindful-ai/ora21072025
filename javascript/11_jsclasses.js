class newString{

    // Data

    this.mainstring = initvalue;

    // Functions
    getindeces(substr){
        var indeces = []
        return indeces;
    }
    
    capitalize(){
    
    }
    
    maketitle(){
    
    }
    
    formatstr(){
    
    }

}


var text = "mary had a little lamb";

var s = new newString(text);
s.capitalize();
s.getindeces('a');
s.formatstr();
s.maketitle()
s.removePunctuations();


var text = 'You might be wondering “what do I say?” or “what if I say the wrong thing?”';
text.removePunctuations();



